# Module Spec Implementation Status

The module specifications in this directory define intended contracts and acceptance criteria. Unless `docs/STATUS.md` explicitly marks a module Implemented, a module spec must not be interpreted as evidence that the feature exists in code.

Implementation progress is tracked separately so product scope is never confused with current software capability.